<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!--
MyBatis核心配置文件中的标签必须要按照指定的顺序配置
(properties?,settings?,typeAliases?,typeHandlers?,objectFactory?,objectWrapperFactory?,reflectorFactory?,plugins?,environments?,databaseIdProvider?,mappers?)

-->

    <!--引入我们的propertise配置文件,引入后就可以在当前文件中使用${key}的方式访问对于的数据-->
    <properties resource="jdbc.properties"/>

    <typeAliases>
        <!--取别名,在MyBatis范围中，type为需要取别名的类型，alies为别名，不设置alies会默认别名即为类名User不区分大小写，
        以后哪个程序要调用该类直接用abc就可以了-->
        <typeAlias type="org.example.pojo.User" alias="abc"></typeAlias>
        <!--通过包设置类型别名，指定包下所有的类型都将全部拥有默认的别名即类名，不区分大小写-->
        <package name="org.example.pojo"/>
    </typeAliases>
    <!-- environments配置连接数据库的环境
     属性 default：来设置默认的使用环境id，不能重复
     -->
    <!--
     transactionManager：设置事务管理器
     type："JDBC|MANAGED"设置食物语的管理方式
     JDBC：表示使用JDBC中原生的事务管理方式
    -->
    <!--
   dataSource ：设置数据源
   type：设置数据源的类型
   type=“POOLED|UNPOOLED|JNDI”
   POOLED：表示使用数据库连接池
   UNPOOLED：表示不适用数据库连接池
   JNDI：表示使用上下文中的数据源
  -->

    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/ssm?serverTimezone=UTC"/>
                <property name="username" value="root"/>
                <property name="password" value="123456"/>
            </dataSource>
        </environment>

     <!--test所用到的环境-->
        <environment id="test">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/ssm?serverTimezone=UTC"/>
                <property name="username" value="root"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>



    <!--  引入mybatis的映射文件-->
    <mappers>
      <!-- <mapper resource="mappers/UserMapper.xml"/>-->
        <!--
        以包的方式引入映射文件，但是必须满足两个条件
        1.mapper接口和映射文件所在的包一致
        2.mapper接口的名字和映射文件接口名字一致
        -->
        <package name="org.example.mapper"/>
    </mappers>
</configuration>